import styled from 'styled-components';

export const Container = styled.footer`
  width: 100%;
  height: 100%;
  background-color: var(--blue);

  text {
    color: var(--shape);
    font-size: 0.8rem;
    margin-left: 5rem;
  }
`;