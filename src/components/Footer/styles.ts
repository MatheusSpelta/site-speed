import styled from 'styled-components';

export const Container = styled.footer`
  display: flex;
  width: 100%;
  height: 100%;
  background-color: var(--blue);
  align-items: center;
  justify-content: center;

  text {
    color: var(--shape);
    font-size: 0.8rem;
    text-align: center;
    margin-top: 0.4rem 0.2rem;
  }
`;